# -*- mode: python ; coding: utf-8 -*-


block_cipher = None


a = Analysis(['service_generator.py'],
             pathex=['D:\\DEV\\drive_watcher\\erpy\\prj'],
             binaries=[],
             datas=[],
             hiddenimports=['win32timezone', 'psutil'],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          a.binaries,
          a.zipfiles,
          a.datas,
          [],
          name='service.exe',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          upx_exclude=[],
          runtime_tmpdir=None,
          console=True )
